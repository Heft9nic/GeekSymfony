{% extends 'GeekhubMainBundle::layout.html.twig' %}

{% block content %}
    <h1>Hello, {{ name }}!</h1>
    <h3>{{ serviceResult }}</h3>

    <ul>
        <li>{{ twigArray.key }}</li>
        <li>{{ twigArray.test }}</li>
        <li>{{ twigArray.geek }}</li>
    </ul>
{% endblock %}
